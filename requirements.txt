# Web framework
# Pinned: starlette >=0.41 removed the positional TemplateResponse(name, context)
# form the routes use. Keep on the pre-removal line until routes are migrated to
# TemplateResponse(request, name, context).
fastapi>=0.110.0,<0.112.0
starlette>=0.37.0,<0.38.0
uvicorn[standard]
jinja2>=3.1.0,<3.2.0
python-multipart  # required by FastAPI to parse the admin form POSTs

# Data analysis
# Pinned to the 3.x line (2026-08-13). This was previously unpinned, so every
# Railway build silently took whatever pandas was current — the deploy was
# already on 3.x while CLAUDE.md still described it as "pinned to 2.x". The
# major-version ceiling is the point: this codebase has been bitten repeatedly
# by dtype-strictness changes across a major bump, so a 4.0 must be a deliberate
# migration, not something a rebuild picks up. Verified on 3.0.5 — full suite
# green, and all three dtype hazards in CLAUDE.md behave identically to 2.x.
pandas>=3.0,<4.0
numpy
pyarrow  # parquet engine required by pd.read_parquet (not pulled in by any other dep here)
plotly
markdown>=3.4

# Local dev
python-dotenv

# External services
PyGithub
google-auth
gspread

# LLM / reporting
anthropic
